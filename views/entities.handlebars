<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Upload page</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="container">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h4>Entity List</h4>
            </div>
            <!-- <hr> -->
            <table class="table table-fixed">
                <thead>
                    <tr>
                        <th class="col-xs-2">ID</th>
                        <th class="col-xs-3">Type</th>
                        <th class="col-xs-3">Model Name</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each data}}
                    <tr>
                        <td class="col-xs-2">{{id}}</td>
                        <td class="col-xs-3">{{type}}</td>
                        <td class="col-xs-3">{{ModelName.value}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

        </div>
        <span class="msg">
            <p>{{msg}}</p>
        </span>
    </div>
</body>

</html>